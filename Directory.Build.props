<Project>
  <!-- Optional, git-ignored local package testing. Published samples keep using NuGet.Config. -->
  <Import Project="$(MSBuildThisFileDirectory).codex-build/Directory.Build.local.props" Condition="Exists('$(MSBuildThisFileDirectory).codex-build/Directory.Build.local.props')" />
  <PropertyGroup>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
    <NuGetAudit>true</NuGetAudit>
    <NuGetAuditMode>all</NuGetAuditMode>
    <NuGetAuditLevel>low</NuGetAuditLevel>
    <!-- Normal builds show advisories; release verification treats them as errors. -->
    <DefaultItemExcludes>$(DefaultItemExcludes);**/App_Data/**;**/artifacts/**;**/MyDocuments/**;**/documents/**;**/*.gguf;**/.codex-build/**</DefaultItemExcludes>
  </PropertyGroup>
  <ItemGroup>
    <Content Remove="**/App_Data/**;**/artifacts/**;**/MyDocuments/**;**/documents/**;**/*.gguf;**/*.snk;**/*.pfx;**/*.pem;**/*.key;**/*.env;**/*.db;**/*.db-*" />
    <None Remove="**/App_Data/**;**/artifacts/**;**/MyDocuments/**;**/documents/**;**/*.gguf;**/*.snk;**/*.pfx;**/*.pem;**/*.key;**/*.env;**/*.db;**/*.db-*" />
  </ItemGroup>
</Project>
